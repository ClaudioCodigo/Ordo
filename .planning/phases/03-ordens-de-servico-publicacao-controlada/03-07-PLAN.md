---
phase: 03-ordens-de-servico-publicacao-controlada
plan: 07
type: execute
wave: 7
depends_on: [03-06]
files_modified:
  - app/src/main/java/dev/claudiocodigo/nexo/domain/caldav/EventColorMapping.kt
  - app/src/main/java/dev/claudiocodigo/nexo/domain/caldav/ColorStatePreferences.kt
  - app/src/test/java/dev/claudiocodigo/nexo/domain/caldav/EventColorMappingTest.kt
  - app/src/main/java/dev/claudiocodigo/nexo/ui/screens/sincronizacoes/SyncCenterViewModel.kt
  - app/src/main/java/dev/claudiocodigo/nexo/ui/screens/sincronizacoes/SyncCenterScreen.kt
  - app/src/test/java/dev/claudiocodigo/nexo/ui/screens/sincronizacoes/SyncCenterViewModelTest.kt
  - app/src/main/java/dev/claudiocodigo/nexo/ui/screens/mais/MaisScreen.kt
  - app/src/main/java/dev/claudiocodigo/nexo/ui/navigation/Routes.kt
  - app/src/main/java/dev/claudiocodigo/nexo/MainActivity.kt
  - app/src/androidTest/java/dev/claudiocodigo/nexo/NavigationTest.kt
autonomous: true
requirements: [OS-03, OS-08]
estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "D-16: Central de sincronizações lists pending, sending, failed, conflict and recent sent operations; retry/cancel actions appear only for eligible persisted states."
    - "D-19/D-21: local Awaiting external validation and remote Validated remain distinct; known greens/red seed configurable mappings, blues stay neutral and purple stays unmapped."
    - "D-21: raw event color is always preserved and mapping configuration changes only local classification; red has visual precedence."
    - "R8 edge coverage: no color/empty color is valid, status precedence is deterministic, ordering uses stable timestamps/IDs and repeated observation does not create history rows."
  prohibitions:
    - statement: "Central actions never create publication without a previously confirmed immutable operation."
      status: "enforced"
      verification: "Retry re-enqueues eligible operation ID; cancel only changes pending state."
    - statement: "Sync-center cleanup does not delete canonical OS/version history."
      status: "enforced"
      verification: "Recent display retention is a query limit, not deletion."
    - statement: "Color configuration/classification never writes remote COLOR or converts conclusion into validation."
      status: "enforced"
      verification: "ColorStatePreferences has local-only API and writer remains absent."
    - statement: "Conflict retry cannot bypass renewed preview/confirmation."
      status: "enforced"
      verification: "Conflict rows navigate to review and have no direct retry action."
  artifacts:
    - path: "app/src/main/java/dev/claudiocodigo/nexo/domain/caldav/ColorStatePreferences.kt"
      provides: "Configurable local color-to-state mapping"
    - path: "app/src/main/java/dev/claudiocodigo/nexo/ui/screens/sincronizacoes/SyncCenterScreen.kt"
      provides: "Persisted publication queue/history UI"
  key_links:
    - from: "SyncCenterViewModel"
      to: "PublicationRepository.observeOperations"
      via: "Flow and allowed state transitions"
      pattern: "observe|retry|cancel"
    - from: "EventColorMapping"
      to: "ColorStatePreferences"
      via: "seeded local configurable sets"
      pattern: "VALIDADO|REQUER_ATENCAO"
---

<objective>
Expose publication progress and configurable external color signals without conflating them with local completion or adding remote mutation.

Purpose: make offline/conflict state understandable and actionable from a central place.
Output: configurable local color classifier, sync center and navigation from Mais.
</objective>

<execution_context>
@C:/Users/claudio.lima/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/claudio.lima/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-ordens-de-servico-publicacao-controlada/03-CONTEXT.md
@.planning/phases/03-ordens-de-servico-publicacao-controlada/03-06-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Make external color classification configurable and local-only</name>
  <read_first>EventColorMapping.kt; EventColorMappingTest.kt; 03-CONTEXT.md D-19..D-21</read_first>
  <files>domain/caldav/EventColorMapping.kt, domain/caldav/ColorStatePreferences.kt, test/domain/caldav/EventColorMappingTest.kt</files>
  <behavior>
    - Seeds #008000/#228B22/#32CD32/green/darkolivegreen as validated and #B22222 as attention.
    - #4682B4/#00679E are neutral, #9370DB and unknown/absent colors are unmapped.
    - User mapping overrides local classification only; red/attention wins when local/external signals compete.
    - Raw color string remains unchanged and no method emits a remote mutation.
  </behavior>
  <action>Evolve classifier to accept a local preferences/config provider with normalized aliases while preserving raw values. Seed only observed mappings. Keep neutral/unmapped explicit and make precedence a pure tested function. Do not infer Autosserviço from purple or add color to writer/payload editor.</action>
  <verify><automated>./gradlew testDebugUnitTest --tests '*EventColorMappingTest'</automated></verify>
  <acceptance_criteria>All known/unknown/override/precedence cases pass and no data/caldav writer import exists in color configuration.</acceptance_criteria>
  <done>Irregular server colors can be interpreted honestly without altering the calendar.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Present persisted publication queue and safe actions</name>
  <read_first>PublicationRepository.kt; PublicationScheduler.kt; AgendaViewModel/Screen list patterns; D-16</read_first>
  <files>ui/screens/sincronizacoes/SyncCenterViewModel.kt, ui/screens/sincronizacoes/SyncCenterScreen.kt, test/ui/screens/sincronizacoes/SyncCenterViewModelTest.kt</files>
  <behavior>
    - Sections show pending/sending/failed/conflict/recent sent in stable newest-first order with OS identity and actionable explanation.
    - Pending can cancel; transient failed can retry; conflict opens review; sending/sent/denied do not expose unsafe controls.
    - Retry schedules existing operation, never clones or changes immutable payload; display retention does not delete history.
  </behavior>
  <action>Build Flow-driven ViewModel and keyed LazyColumn screen with typed UI models, clear empty state and stable testTags. Retain a bounded recent-sent display window chosen at the UI query layer. Route conflict by operation/OS ID. Surface forbidden as `Sem permissão para atualizar este evento` and unauthorized as reconnect action without exposing details.</action>
  <verify><automated>./gradlew testDebugUnitTest --tests '*SyncCenterViewModelTest' assembleDebug</automated></verify>
  <acceptance_criteria>All state/action tests pass, duplicate worker emissions do not duplicate rows, and conflict has no direct retry button.</acceptance_criteria>
  <done>The user can understand and safely manage every persisted synchronization state.</done>
</task>

<task type="auto">
  <name>Task 3: Add Central de sincronizações to Mais and typed navigation</name>
  <read_first>MaisScreen.kt; Routes.kt; MainActivity.kt; NavigationTest.kt; SyncCenterScreen.kt</read_first>
  <files>ui/screens/mais/MaisScreen.kt, ui/navigation/Routes.kt, MainActivity.kt, androidTest/NavigationTest.kt</files>
  <action>Add a clearly labeled Central de sincronizações entry under Mais, its full-screen typed route, bottom-bar hiding and navigation callbacks for OS/editor/conflict targets. Route arguments contain IDs only. Preserve existing account/settings actions and adaptive navigation behavior.</action>
  <verify><automated>./gradlew assembleDebugAndroidTest</automated></verify>
  <acceptance_criteria>Navigation test reaches Mais→Central, opens a conflict review by ID, returns safely and never carries ICS/description in route serialization.</acceptance_criteria>
  <done>All queued and recent publication operations are reachable from the app's primary navigation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

ASVS level 1; block on high. Persisted error/state data crosses Room→UI and local configuration affects status classification.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-03-31 | Tampering | retry/cancel UI | high | mitigate | typed allowed transitions and repository-enforced state checks |
| T-03-32 | Spoofing | color classification | medium | mitigate | explicit configurable mapping, neutral unknowns and raw preservation |
| T-03-33 | Information disclosure | error/list UI | medium | mitigate | sanitized messages and ID-only routes |
| T-03-34 | Repudiation | recent-history display | low | accept | canonical immutable history remains in Room; UI retention is presentation only |
| T-03-SC | Tampering | package installation | low | accept | no package installation |
</threat_model>

<verification>
Focused classifier/ViewModel tests, then instrumented navigation compilation. Wave gate `./gradlew testDebugUnitTest assembleDebug assembleDebugAndroidTest`.
</verification>

<success_criteria>
- Every queue state is visible and only safe transitions are offered.
- Color signals are configurable, raw-preserving and local-only.
- Awaiting external validation and validated externally remain distinct.
- Central is reachable from Mais without sensitive route payloads.
</success_criteria>

## Artifacts this phase produces

- Configurable `ColorStatePreferences` and evolved `EventColorMapping`.
- `SyncCenterViewModel`, `SyncCenterScreen`, typed sync-center route and navigation tests.

<output>
Create `.planning/phases/03-ordens-de-servico-publicacao-controlada/03-07-SUMMARY.md` when done.
</output>
