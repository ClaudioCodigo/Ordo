---
phase: 03-ordens-de-servico-publicacao-controlada
plan: 08
type: execute
wave: 8
depends_on: [03-07]
files_modified:
  - app/src/main/java/dev/claudiocodigo/nexo/domain/serviceorder/OperationalOrderProjection.kt
  - app/src/test/java/dev/claudiocodigo/nexo/domain/serviceorder/OperationalOrderProjectionTest.kt
  - app/src/main/java/dev/claudiocodigo/nexo/ui/screens/hoje/HojeViewModel.kt
  - app/src/main/java/dev/claudiocodigo/nexo/ui/screens/hoje/HojeScreen.kt
  - app/src/test/java/dev/claudiocodigo/nexo/ui/screens/hoje/HojeViewModelTest.kt
  - app/src/main/java/dev/claudiocodigo/nexo/ui/screens/agenda/AgendaViewModel.kt
  - app/src/main/java/dev/claudiocodigo/nexo/ui/screens/agenda/AgendaScreen.kt
  - app/src/test/java/dev/claudiocodigo/nexo/ui/screens/agenda/AgendaViewModelTest.kt
  - app/src/test/java/dev/claudiocodigo/nexo/ui/screens/agenda/AgendaRemoteViewModelTest.kt
autonomous: true
requirements: [OS-02, OS-03, OS-08]
estimate:
  tokens: 27000
  raw_tokens: 27000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "D-02: linked local+remote records project as one operational card while remaining separate Room records; tapping linked card opens remote detail first."
    - "D-16: card badges show Waiting connection, Sending, Failed or Conflict from persisted outbox state."
    - "D-19: internally completed without mapped green shows Awaiting external validation; mapped green shows Validated externally."
    - "D-20/D-21: mapped red has highest precedence as Requires attention; history stays intact and Reopen attendance explicitly adds a new update stage in the same OS."
    - "R8 edge coverage: absent color is valid, status precedence/order is deterministic and repeated projections create no history rows."
  prohibitions:
    - statement: "Card observation/opening never confirms or publishes an operation."
      status: "enforced"
      verification: "Projection/ViewModel tests observe zero outbox inserts/writer calls."
    - statement: "Linked-card unification never deletes or physically merges local/remote/history records."
      status: "enforced"
      verification: "Projection is read-only combine; repository rows remain separately queryable."
    - statement: "Internal completion never renders as external validation without mapped green; Nexo never writes green."
      status: "enforced"
      verification: "Status projection tests all local/color combinations; no writer dependency."
    - statement: "Conflict card action cannot resend stale operation directly."
      status: "enforced"
      verification: "Conflict action navigates only to conflict review."
  artifacts:
    - path: "app/src/main/java/dev/claudiocodigo/nexo/domain/serviceorder/OperationalOrderProjection.kt"
      provides: "Single-card join and status precedence"
  key_links:
    - from: "HojeViewModel/AgendaViewModel"
      to: "OperationalOrderProjection"
      via: "combine remote/local/outbox/color Flows"
      pattern: "combine"
    - from: "linked OperationalOrderCard"
      to: "Route.EventoRemoto"
      via: "remote detail first; local ID retained for Continue"
      pattern: "EventoRemoto"
---

<objective>
Unify remote events, local work and publication status into one honest operational card in Hoje and Agenda, including explicit reopening after red return.

Purpose: prevent duplicate/confusing entries while preserving every underlying record and external signal.
Output: pure projection, status precedence and updated Today/Agenda screens/tests.
</objective>

<execution_context>
@C:/Users/claudio.lima/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/claudio.lima/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-ordens-de-servico-publicacao-controlada/03-CONTEXT.md
@.planning/phases/03-ordens-de-servico-publicacao-controlada/03-07-SUMMARY.md

<interfaces>
Inputs are separate Flows of remote resources/occurrences, local OS links, outbox state and local color classification. Output is a read-only `OperationalOrderCard` carrying route IDs and derived status only.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Derive one card and deterministic operational status</name>
  <read_first>HojeViewModel.kt; AgendaViewModel.kt; EventColorMapping.kt; PublicationModels.kt; D-02/D-16/D-19..D-21</read_first>
  <files>domain/serviceorder/OperationalOrderProjection.kt, test/domain/serviceorder/OperationalOrderProjectionTest.kt</files>
  <behavior>
    - Exact linked remote/local pair yields one card; unlinked remote and provisional local each yield one card; similar UID/title never auto-links.
    - Precedence is red Requires attention > conflict > sending/pending/failure > mapped green Validated externally > internal completion Awaiting external validation > ordinary progress.
    - Card target is remote detail for linked/remote and editor for provisional local; conflict target is review.
    - Projection is stable/newest-first with ID tie-break and has no persistence side effects.
  </behavior>
  <action>Create a pure projection over explicit identity/link records. Keep user-facing title/OS number/local and queue status without merging entity ownership. Implement exact status precedence and route intent as typed values. Do not perform fuzzy deduplication or infer purple as autoservice.</action>
  <verify><automated>./gradlew testDebugUnitTest --tests '*OperationalOrderProjectionTest'</automated></verify>
  <acceptance_criteria>Matrix covers linked/unlinked/provisional, every status precedence combination, null/unknown colors, stable ordering and zero mutation.</acceptance_criteria>
  <done>All list screens can consume one consistent operational truth.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Use unified cards and explicit reopen in Hoje</name>
  <read_first>OperationalOrderProjection.kt; HojeViewModel.kt; HojeScreen.kt; existing 30-day attention filter tests</read_first>
  <files>ui/screens/hoje/HojeViewModel.kt, ui/screens/hoje/HojeScreen.kt, test/ui/screens/hoje/HojeViewModelTest.kt</files>
  <behavior>
    - Linked records display once with badges; newest/current-day ordering remains intuitive.
    - Red completed/validated card shows Requires attention and explicit Reopen attendance; action adds one new update stage then opens same OS.
    - Existing rule continues to omit red events aged 30 days or more from Hoje without deleting/hiding them in Agenda.
  </behavior>
  <action>Replace separate local/remote list assembly with combined projection Flow. Render badges and explanatory copy, preserve existing design and old-event filter. Wire Reopen to a transactional new update stage, never automatic status reset; card tap still opens remote detail for linked work.</action>
  <verify><automated>./gradlew testDebugUnitTest --tests '*HojeViewModelTest' assembleDebug</automated></verify>
  <acceptance_criteria>Tests cover single linked card, all badges, red precedence/reopen idempotency and unchanged 30-day Hoje filter.</acceptance_criteria>
  <done>Hoje points the technician to current work without duplicate or misleading completion state.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Use the same projection and newest-first behavior in Agenda</name>
  <read_first>OperationalOrderProjection.kt; AgendaViewModel.kt; AgendaScreen.kt; AgendaRemoteViewModelTest.kt</read_first>
  <files>ui/screens/agenda/AgendaViewModel.kt, ui/screens/agenda/AgendaScreen.kt, test/ui/screens/agenda/AgendaViewModelTest.kt, test/ui/screens/agenda/AgendaRemoteViewModelTest.kt</files>
  <behavior>
    - Agenda displays one linked card, all historical/old events, and newest-first ordering with stable tie-break.
    - Search/filter works over remote and structured local fields without creating relationships.
    - Badge/actions match Hoje; old Requires attention remains visible here and can explicitly reopen.
  </behavior>
  <action>Adopt the shared projection, retain Agenda's complete-history semantics and search, and remove duplicate local/remote card assembly. Reuse the same badge/action component or semantics so status cannot diverge between tabs. Preserve remote-detail-first navigation.</action>
  <verify><automated>./gradlew testDebugUnitTest --tests '*AgendaViewModelTest' --tests '*AgendaRemoteViewModelTest' assembleDebug</automated></verify>
  <acceptance_criteria>Agenda tests prove no duplicate link, newest-first ordering, old-red visibility, search and status parity with Hoje.</acceptance_criteria>
  <done>Hoje and Agenda present the same OS identity/status rules while retaining their different time scopes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

ASVS level 1; block on high. Multiple persisted sources and external color evidence cross into user-visible operational decisions.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-03-35 | Spoofing | linked-card identity | high | mitigate | exact composite link only; no UID/title fuzzy merge |
| T-03-36 | Tampering | status precedence | high | mitigate | pure exhaustive matrix with red/conflict precedence tests |
| T-03-37 | Repudiation | reopen action | medium | mitigate | explicit action creates timestamped update stage in same history |
| T-03-38 | Denial of service | combined list Flow | low | accept | bounded local Room flows and keyed LazyColumn; existing dataset scale is small |
| T-03-SC | Tampering | package installation | low | accept | no package installation |
</threat_model>

<verification>
Run projection matrix, then focused Hoje/Agenda tests. Wave gate `./gradlew testDebugUnitTest assembleDebug`.
</verification>

<success_criteria>
- Linked work appears exactly once in both tabs.
- Publication/external/local states are distinguishable with fixed precedence.
- Red return never erases completion and reopening is explicit.
- Hoje filters old attention events while Agenda preserves them.
</success_criteria>

## Artifacts this phase produces

- `OperationalOrderProjection`, `OperationalOrderCard`, `OperationalStatus`, route intents and exhaustive matrix tests.
- Evolved `HojeViewModel/Screen` and `AgendaViewModel/Screen` using the shared projection.

<output>
Create `.planning/phases/03-ordens-de-servico-publicacao-controlada/03-08-SUMMARY.md` when done.
</output>
