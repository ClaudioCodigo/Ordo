---
phase: 03-ordens-de-servico-publicacao-controlada
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/main/java/dev/claudiocodigo/nexo/domain/serviceorder/ServiceOrderModels.kt
  - app/src/main/java/dev/claudiocodigo/nexo/data/local/entity/ServiceOrderRecords.kt
  - app/src/main/java/dev/claudiocodigo/nexo/data/local/dao/ServiceOrderStoreDao.kt
  - app/src/main/java/dev/claudiocodigo/nexo/data/repository/RoomServiceOrderRepository.kt
  - app/src/main/java/dev/claudiocodigo/nexo/data/local/NexoDatabase.kt
  - app/src/main/java/dev/claudiocodigo/nexo/data/local/NexoDatabaseMigrations.kt
  - app/src/main/java/dev/claudiocodigo/nexo/di/DatabaseModule.kt
  - app/schemas/dev.claudiocodigo.nexo.data.local.NexoDatabase/3.json
  - app/src/androidTest/java/dev/claudiocodigo/nexo/data/local/NexoDatabaseMigrationTest.kt
  - app/src/androidTest/java/dev/claudiocodigo/nexo/data/repository/RoomServiceOrderRepositoryTest.kt
autonomous: false
requirements: [OS-01, OS-02, OS-04, OS-06, OS-07, OS-08]
estimate:
  tokens: 46000
  raw_tokens: 46000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "D-01/D-02/D-03: iniciar atendimento é explícito, create-or-get por conta+calendário+href+ocorrência devolve sempre a mesma OS, e UID isolado nunca deduplica recursos."
    - "D-04/D-05/D-06: a OS vinculada mantém SUMMARY, DESCRIPTION, ICS e ETag brutos em snapshots separados do rascunho; uma leitura remota tardia não substitui edição local mais nova."
    - "R1 edge coverage: texto vazio/Unicode é preservado, vínculo composto é idempotente e concorrência de autosave converge para a revisão local mais nova."
    - "R2 edge coverage: UUID local, UID de publicação e identidade da operação são estáveis; número oficial ausente aparece como SEM OS sem virar identidade."
    - "R4/R5/R8 edge coverage: updates têm UUID e ordem estável, finalização não apaga dados, snapshots/versões têm unicidade e sincronizações repetidas não duplicam histórico."
    - "Migrações 1→3 e 2→3 são explícitas, não destrutivas e preservam byte a byte rascunhos da Fase 1 e cache da Fase 2."
  prohibitions:
    - statement: "O Nexo não publica evento ou atualização sem confirmação explícita."
      status: "enforced"
      verification: "O storage não aciona rede; testes de repositório observam zero cliente mutante."
    - statement: "O Nexo não apaga ou substitui silenciosamente demanda, rascunho ou versão histórica."
      status: "enforced"
      verification: "Testes DAO/migração consultam todas as entidades após concluir e migrar."
    - statement: "Conclusão interna não equivale a validação externa e o Nexo não aplica verde."
      status: "enforced"
      verification: "Estados locais e sinal remoto são colunas/tipos separados, sem campo de mutação de cor."
    - statement: "O Nexo não reenvia cegamente após 412 nem marca versão desatualizada como publicada."
      status: "enforced"
      verification: "O modelo reserva estados de conflito separados; comportamento remoto entra nos planos 04/06."
  artifacts:
    - path: "app/src/main/java/dev/claudiocodigo/nexo/domain/serviceorder/ServiceOrderModels.kt"
      provides: "Aggregate estruturado, occurrence key, snapshots, updates, itens, versões e estados"
    - path: "app/src/main/java/dev/claudiocodigo/nexo/data/local/entity/ServiceOrderRecords.kt"
      provides: "Tabelas Room aditivas e índices de idempotência"
    - path: "app/schemas/dev.claudiocodigo.nexo.data.local.NexoDatabase/3.json"
      provides: "Schema Room v3 exportado"
  key_links:
    - from: "RoomServiceOrderRepository"
      to: "ServiceOrderStoreDao"
      via: "transações create-or-get/snapshot/draft/version"
      pattern: "withTransaction|createOrGet|insert.*IGNORE"
    - from: "NexoDatabaseMigrations.MIGRATION_2_3"
      to: "NexoDatabase version 3"
      via: "SQL equivalente ao schema exportado"
      pattern: "MIGRATION_2_3|version = 3"
---

<objective>
Entregar o tracer local da Fase 3: uma ocorrência remota vira exatamente uma OS estruturada e recuperável, com snapshot/ETag, histórico e dados editáveis persistidos em Room.

Purpose: provar identidade, separação de versões e migração antes de qualquer capacidade de PUT.
Output: domínio estruturado, store Room v3, migrações verificadas e create-or-get idempotente.
</objective>

<execution_context>
@C:/Users/claudio.lima/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/claudio.lima/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/03-ordens-de-servico-publicacao-controlada/03-SPEC.md
@.planning/phases/03-ordens-de-servico-publicacao-controlada/03-CONTEXT.md
@.planning/phases/03-ordens-de-servico-publicacao-controlada/03-RESEARCH.md
@.planning/phases/03-ordens-de-servico-publicacao-controlada/03-PATTERNS.md

<interfaces>
Existing `ServiceOrderRepository` exposes Flow list, get by UUID, save and delete. Evolve it without introducing network dependencies. Existing `RemoteEventEntity` resource identity is accountId+calendarHref+href; retain it and make occurrence identity an explicit child key using normalized RECURRENCE-ID.
</interfaces>

<execution_note>Although the plan's file count is high, 03-01 is cohesive migration/model work with focused ownership per task. The related 03-02 file count is likewise cohesive parser/lossless-fixture work and intentionally depends on 03-01; do not split either plan solely by file count.</execution_note>
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Trace one remote occurrence into one durable structured OS</name>
  <read_first>03-SPEC.md R1/R8; 03-CONTEXT.md D-01..D-06; ServiceOrder.kt; ServiceOrderRepository.kt; RoomServiceOrderRepository.kt; RemoteEventEntity.kt</read_first>
  <files>domain/serviceorder/ServiceOrderModels.kt, data/local/entity/ServiceOrderRecords.kt, data/local/dao/ServiceOrderStoreDao.kt, data/repository/RoomServiceOrderRepository.kt, androidTest/data/repository/RoomServiceOrderRepositoryTest.kt</files>
  <behavior>
    - Given the same account/calendar/href/UID/RECURRENCE-ID twice, createOrGetAttendance returns the same local UUID and does not add another link (D-02/D-03).
    - Two explicit occurrences with one UID and different RECURRENCE-ID values create different links; two hrefs with the same UID remain different resources.
    - A linked draft, immutable base snapshot and current remote candidate remain separate; completing an older load cannot overwrite a newer saved revision (D-05).
    - Official number null/????/SEM OS remains absent while the local UUID stays stable (OS-02).
  </behavior>
  <action>Write the failing in-memory Room repository test first, then add the structured aggregate, a `StructuredServiceOrderStore` contract beside the domain models, and the narrow repository commands needed for the single create-or-get path. Model preset, origin text, updates, pendings, generic items, completion, remote occurrence key, immutable base snapshot, version identity and a calendar-capability record that can retain exact DAV privilege names. Keep Room annotations in data types and tolerant enum mapping at the boundary. The repository must use a transaction/unique index for create-or-get, not query-then-insert in application code. This is the production skeleton consumed by every later plan.</action>
  <verify>
    <automated>./gradlew connectedDebugAndroidTest -Pandroid.testInstrumentationRunnerArguments.class=dev.claudiocodigo.nexo.data.repository.RoomServiceOrderRepositoryTest</automated>
  </verify>
  <acceptance_criteria>All four behaviors pass; repository has no CalDAV writer dependency; raw remote evidence and mutable draft are independently queryable.</acceptance_criteria>
  <done>One remote occurrence can be transformed into, persisted as, and recovered as exactly one structured OS end-to-end through the real Room DAO/repository path.</done>
</task>

<task type="checkpoint:decision" gate="blocking-human">
  <name>Checkpoint: Approve applying the schema v3 migration</name>
  <decision>Approve applying the schema v3 migration after migration tests are green?</decision>
  <context>Schema v3 changes persisted user data and is a one-way operational step for existing installations. The migration task must not run until the automated migration tests are green and you explicitly approve applying it.</context>
  <options>
    <option id="approve-schema-v3">
      <name>Approve schema v3 migration</name>
      <pros>Allows the tested additive migration to be applied to persisted installations.</pros>
      <cons>Future changes require forward migrations; downgrade requires an explicit reverse migration/app rollback strategy.</cons>
    </option>
    <option id="hold-schema-v3">
      <name>Hold migration</name>
      <pros>Keeps persisted installations unchanged while the migration decision is deferred.</pros>
      <cons>Task 2 cannot proceed until explicit approval is provided.</cons>
    </option>
  </options>
  <resume-signal>Type exactly “aprovar migração v3” to approve after migration tests are green, or “adiar migração v3” to hold. Downgrade requires an explicit reverse migration/app rollback strategy.</resume-signal>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Ship additive Room v3 migrations with preservation proofs</name>
  <read_first>NexoDatabase.kt; NexoDatabaseMigrations.kt; DatabaseModule.kt; schemas 1.json and 2.json; NexoDatabaseMigrationTest.kt</read_first>
  <reversibility rating="one-way">Schema v3 becomes persisted user data; later changes require forward migrations, but its structure is already locked by the phase decisions.</reversibility>
  <files>data/local/NexoDatabase.kt, data/local/NexoDatabaseMigrations.kt, di/DatabaseModule.kt, app/schemas/dev.claudiocodigo.nexo.data.local.NexoDatabase/3.json, androidTest/data/local/NexoDatabaseMigrationTest.kt</files>
  <behavior>
    - Seeded v1 service_orders survive 1→2→3 with original values.
    - Seeded v2 account/calendar/remote_events/sync state and service orders survive 2→3.
    - New occurrence/link/update/item/snapshot/version/outbox tables and all unique/index constraints validate against schema 3.
    - No destructive fallback or drop/recreate of legacy tables exists.
  </behavior>
  <action>Register every Phase 3 storage record in NexoDatabase version 3, add MIGRATION_2_3 while retaining MIGRATION_1_2, register migrations in DatabaseModule, and export schema 3. Generate the schema via the existing Room build rather than editing JSON by hand. Tests must exercise direct 2→3 and the composed 1→3 path; the cross-file JVM consistency gate is added in the final validation plan after the schema stabilizes.</action>
  <verify>
    <automated>./gradlew assembleDebugAndroidTest</automated>
    <human-check>On an attached device/emulator, run the two migration cases with connectedDebugAndroidTest before closing the phase.</human-check>
  </verify>
  <acceptance_criteria>Schema 3 is exported; both migration paths compile and validate; legacy values remain byte-equivalent; no destructive migration API is present.</acceptance_criteria>
  <done>Existing installations can upgrade to the structured store without losing Phase 1 or Phase 2 data.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Complete aggregate persistence semantics and stable history ordering</name>
  <read_first>ServiceOrderModels.kt; ServiceOrderStoreDao.kt; RoomServiceOrderRepositoryTest.kt; 03-SPEC.md R3-R5/R8</read_first>
  <files>data/local/dao/ServiceOrderStoreDao.kt, data/repository/RoomServiceOrderRepository.kt, androidTest/data/repository/RoomServiceOrderRepositoryTest.kt</files>
  <behavior>
    - Updates with equal text and different UUIDs remain distinct, sorted by executionAt, createdAt and UUID.
    - Finishing creates one immutable historical version but preserves origin, updates and items locally.
    - Repeating the same save/version operation is idempotent by operation/version identity.
    - Recent technician and company/unit selections can be read without becoming canonical identity.
  </behavior>
  <action>Finish transactional commands and Flow projections for autosave, updates, items, finalization evidence, snapshots and version history. Add stable tie-break ordering in SQL or repository mapping and uniqueness constraints for one version per operation. Preserve existing delete-draft behavior only for purely local drafts; linked history must not be cascade-deleted by ordinary editor actions.</action>
  <verify>
    <automated>./gradlew connectedDebugAndroidTest -Pandroid.testInstrumentationRunnerArguments.class=dev.claudiocodigo.nexo.data.repository.RoomServiceOrderRepositoryTest</automated>
  </verify>
  <acceptance_criteria>Ordering, idempotency, finalization preservation and retention tests pass against Room; no network is required for any command.</acceptance_criteria>
  <done>The local aggregate supplies every durable invariant needed by editor, publication and conflict plans.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

ASVS level 1; block on high. Untrusted ICS-derived identity crosses into Room, and pre-existing databases cross the migration boundary.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-03-01 | Tampering | occurrence/link creation | high | mitigate | composite unique keys plus transactional create-or-get and concurrency tests |
| T-03-02 | Information disclosure | snapshots/history/outbox schema | high | mitigate | store no credential/Authorization fields; later SecurityPolicyTest scans schema and sources |
| T-03-03 | Denial of service | oversized stored raw ICS | medium | mitigate | bounded input contract recorded in models and enforced by extraction plan before insert |
| T-03-04 | Repudiation | immutable versions | medium | mitigate | UUID, timestamps, base ETag and operation relation with stable ordering |
| T-03-05 | Tampering | database migration | high | mitigate | explicit 1→3/2→3 tests and exported schema; no destructive fallback |
| T-03-SC | Tampering | package installation | low | accept | no packages are installed; existing locked dependencies only |
</threat_model>

<verification>
Run focused repository/migration checks after each task. Wave gate: `./gradlew testDebugUnitTest assembleDebug assembleDebugAndroidTest`; if the shell hits the documented Windows loopback/daemon error, rerun from Android Studio's bundled JDK/terminal and record that environment in the SUMMARY rather than weakening tests.
</verification>

<success_criteria>
- Repeated start-attendance commands produce one OS per exact occurrence.
- Raw/base/draft/version records remain separate and queryable.
- Room v3 upgrades from both supported starting schemas without loss.
- All local operations work offline and expose no remote mutation path.
</success_criteria>

## Artifacts this phase produces

- `ServiceOrderDraft`, `RemoteOccurrenceKey`, `ServiceOrderUpdate`, `ServiceOrderItem`, `RemoteBaseSnapshot`, `ServiceOrderVersion`, `PublicationState` in `ServiceOrderModels.kt`.
- Room records/indices in `ServiceOrderRecords.kt`; `ServiceOrderStoreDao`; evolved `ServiceOrderRepository`/`RoomServiceOrderRepository`.
- `NexoDatabase` schema v3, `MIGRATION_2_3`, schema `3.json`, direct and composed migration tests.

<output>
Create `.planning/phases/03-ordens-de-servico-publicacao-controlada/03-01-SUMMARY.md` when done.
</output>
