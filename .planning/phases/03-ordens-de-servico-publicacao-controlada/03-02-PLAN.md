---
phase: 03-ordens-de-servico-publicacao-controlada
plan: 02
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - app/src/main/java/dev/claudiocodigo/nexo/domain/serviceorder/ServiceOrderExtractor.kt
  - app/src/main/java/dev/claudiocodigo/nexo/domain/serviceorder/ServiceOrderRenderer.kt
  - app/src/main/java/dev/claudiocodigo/nexo/domain/serviceorder/ServiceOrderValidation.kt
  - app/src/test/java/dev/claudiocodigo/nexo/domain/serviceorder/ServiceOrderExtractionTest.kt
  - app/src/test/java/dev/claudiocodigo/nexo/domain/serviceorder/ServiceOrderRendererTest.kt
  - app/src/main/java/dev/claudiocodigo/nexo/data/ical/IcsDocument.kt
  - app/src/main/java/dev/claudiocodigo/nexo/data/ical/IcsDocumentEditor.kt
  - app/src/test/java/dev/claudiocodigo/nexo/data/ical/IcsDocumentEditorTest.kt
  - app/src/test/resources/ical/anonymous-simple-folded.ics
  - app/src/test/resources/ical/anonymous-recurring-invited.ics
  - app/src/main/java/dev/claudiocodigo/nexo/data/local/entity/RemoteEventOccurrenceEntity.kt
  - app/src/main/java/dev/claudiocodigo/nexo/data/local/dao/RemoteEventOccurrenceDao.kt
  - app/src/main/java/dev/claudiocodigo/nexo/data/caldav/RemoteEventMapper.kt
  - app/src/test/java/dev/claudiocodigo/nexo/data/caldav/RemoteEventMapperTest.kt
autonomous: true
requirements: [OS-01, OS-05, OS-06, OS-07, OS-08]
estimate:
  tokens: 48000
  raw_tokens: 48000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "D-04/D-06: SUMMARY/DESCRIPTION extraction is deterministic, conservative and raw-preserving; unstructured description enters the selected preset origin field intact."
    - "D-08/D-09/D-10/D-11: both required presets, generic items and mandatory execution date render deterministically; preview content is generated, not freely edited; time is absent from DESCRIPTION."
    - "D-03/D-22: UID+RECURRENCE-ID selects one explicit occurrence while master, sibling occurrence, ORGANIZER, ATTENDEE, PARTSTAT, RSVP, alarms, attachments and unknown properties survive unchanged."
    - "R3 edge coverage: empty required fields block publication but not autosave; Unicode survives; textual/event date divergence is surfaced without mutation."
    - "R4 edge coverage: update UUIDs preserve equal-text records, empty updates are invalid, and ordering uses execution time, creation time and UUID."
    - "R5 edge coverage: completion validates the preset-specific fields, accepts explicit Nenhuma pendency, and does not erase local updates."
    - "R6 edge coverage: absent COLOR/LOCATION stay absent, repeated previews are pure, UTF-8 folding is valid and unchanged physical lines remain exact."
  prohibitions:
    - statement: "No publication occurs without explicit confirmation."
      status: "enforced"
      verification: "Extractor/renderer/editor are pure and have no HTTP dependency."
    - statement: "No demand, draft or historical version is discarded to simplify output."
      status: "enforced"
      verification: "Final renderer changes projection only; domain records remain inputs and tests assert preservation."
    - statement: "No internal completion is rendered as external green validation."
      status: "enforced"
      verification: "Renderer emits Estado: Concluído only and never emits/mutates COLOR."
    - statement: "No stale payload is automatically retried after 412."
      status: "enforced"
      verification: "ICS editor has no transport or retry behavior."
  artifacts:
    - path: "app/src/main/java/dev/claudiocodigo/nexo/domain/serviceorder/ServiceOrderExtractor.kt"
      provides: "Confidence-bearing deterministic extraction"
    - path: "app/src/main/java/dev/claudiocodigo/nexo/domain/serviceorder/ServiceOrderRenderer.kt"
      provides: "Update and completion projections"
    - path: "app/src/main/java/dev/claudiocodigo/nexo/data/ical/IcsDocumentEditor.kt"
      provides: "Lossless surgical VEVENT editor"
  key_links:
    - from: "ServiceOrderRenderer"
      to: "ServiceOrderDraft/updates/items"
      via: "pure immutable render input"
      pattern: "renderUpdate|renderCompletion"
    - from: "IcsDocumentEditor"
      to: "UID + RECURRENCE-ID"
      via: "exact VEVENT component selector"
      pattern: "recurrenceId|selectEvent"
---

<objective>
Turn raw calendar content and structured drafts into safe, deterministic previews while proving lossless ICS mutation before any writer exists.

Purpose: make the user's clean OS format reliable without destroying server-owned event data.
Output: extraction, validation, renderers, anonymous golden fixtures, explicit occurrence indexing and surgical ICS editing.
</objective>

<execution_context>
@C:/Users/claudio.lima/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/claudio.lima/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-ordens-de-servico-publicacao-controlada/03-SPEC.md
@.planning/phases/03-ordens-de-servico-publicacao-controlada/03-CONTEXT.md
@.planning/phases/03-ordens-de-servico-publicacao-controlada/03-RESEARCH.md
@.planning/phases/03-ordens-de-servico-publicacao-controlada/03-01-SUMMARY.md

<interfaces>
Consume `ServiceOrderDraft`, `RemoteOccurrenceKey`, `ServiceOrderUpdate`, `ServiceOrderItem` and snapshot/version contracts from Plan 01. Existing `IcsCalendar.rawIcs` is an extraction view; it is not a writable document.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extract, validate and render the two structured OS presets</name>
  <read_first>03-CONTEXT.md D-04/D-06/D-08..D-11; 03-SPEC.md R1/R3-R5; IcsParser.kt; ServiceOrderModels.kt</read_first>
  <files>domain/serviceorder/ServiceOrderExtractor.kt, domain/serviceorder/ServiceOrderRenderer.kt, domain/serviceorder/ServiceOrderValidation.kt, test/domain/serviceorder/ServiceOrderExtractionTest.kt, test/domain/serviceorder/ServiceOrderRendererTest.kt</files>
  <behavior>
    - SUMMARY supports blank, accented, extra segments, ???? and SEM OS without inventing fields or losing raw text.
    - Structured DESCRIPTION parses known first-colon labels; unstructured text becomes the entire Demanda/Solicitação/Problema relatado selected by the user.
    - Update render includes identification, origin, each update once in stable order and current pendencies; completion render includes origin, Estado: Concluído, preset-required result fields and pendencies but omits the remote timeline.
    - Execution date is mandatory in text; start/end remain native metadata; divergence produces a warning requiring explicit acknowledgement.
  </behavior>
  <action>Write golden tests first. Implement pure Kotlin extraction with per-field confidence/ambiguity, explicit presets `DIAGNOSTICO_CORRECAO` and `SERVICO_SOLICITADO`, preset-aware validation, update renderer and completion renderer. Keep raw text alongside every extraction result. Render optional generic equipment records using action/type/brand/model/serial/related equipment/location/note. Use injected clock/value inputs rather than `System.currentTimeMillis()` inside transforms.</action>
  <verify><automated>./gradlew testDebugUnitTest --tests '*ServiceOrderExtractionTest' --tests '*ServiceOrderRendererTest'</automated></verify>
  <acceptance_criteria>Golden tests cover all behaviors, repeated render returns identical text, equal-text updates with different IDs both appear once, and no time appears in DESCRIPTION.</acceptance_criteria>
  <done>Raw or already structured calendar descriptions can be edited as fields and projected into the approved clean output without duplication.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove lossless occurrence-specific ICS editing with anonymous fixtures</name>
  <read_first>03-SPEC.md R6; 03-COVERAGE.md; IcsModel.kt; IcsParser.kt; RFC constraints summarized in 03-RESEARCH.md Pattern 2</read_first>
  <files>data/ical/IcsDocument.kt, data/ical/IcsDocumentEditor.kt, test/data/ical/IcsDocumentEditorTest.kt, test/resources/ical/anonymous-simple-folded.ics, test/resources/ical/anonymous-recurring-invited.ics</files>
  <behavior>
    - Target one VEVENT by UID+RECURRENCE-ID and mutate only DESCRIPTION, SEQUENCE, DTSTAMP and LAST-MODIFIED.
    - Physical lines outside authorized properties remain byte-identical, including VTIMEZONE, sibling VEVENT, VALARM, ATTACH, ORGANIZER/ATTENDEE parameters and X-properties.
    - Missing COLOR/LOCATION remain absent; Unicode TEXT escapes and folding never split UTF-8 code points and respect 75-octet continuation.
    - Malformed, ambiguous-target or oversized input fails closed without emitting a partial document.
  </behavior>
  <action>Create only synthetic fixtures with reserved domains/names and no real ICS content. Test exact unchanged regions before implementation. Build a tokenized physical-line/component document that preserves original delimiters/order and a surgical editor for the four allowed properties. Do not reconstruct through `IcsEvent`, normalize whole documents, expand RRULE, or expose color/scheduling mutation.</action>
  <verify><automated>./gradlew testDebugUnitTest --tests '*IcsDocumentEditorTest'</automated></verify>
  <acceptance_criteria>All lossless/negative cases pass; fixture scan contains no production hostname, account, real OS number or credential-shaped value.</acceptance_criteria>
  <done>A safe ICS payload can be produced for one explicit occurrence without collateral event changes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Index explicit recurrence components without expanding RRULE</name>
  <read_first>RemoteEventMapper.kt; RemoteEventEntity.kt; RemoteEventDao.kt; 03-RESEARCH.md Pattern 1; anonymous recurring fixture</read_first>
  <files>data/local/entity/RemoteEventOccurrenceEntity.kt, data/local/dao/RemoteEventOccurrenceDao.kt, data/caldav/RemoteEventMapper.kt, test/data/caldav/RemoteEventMapperTest.kt</files>
  <behavior>
    - A VCALENDAR containing master plus two explicit exceptions yields three occurrence rows tied to one resource.
    - The master uses explicit absence of recurrence ID; exceptions retain normalized RECURRENCE-ID and timezone semantics.
    - Resource raw ICS/ETag remains canonical and sync of the same ETag does not duplicate occurrence rows.
  </behavior>
  <action>Replace master-first mapping with a resource-plus-explicit-components projection. Persist each explicit VEVENT extraction under its resource identity and occurrence key while keeping raw ICS only as canonical resource evidence. Upsert all components transactionally during read sync. Do not generate implicit RRULE instances.</action>
  <verify><automated>./gradlew testDebugUnitTest --tests '*RemoteEventMapperTest' assembleDebug</automated></verify>
  <acceptance_criteria>Master/exception tests pass, no UID uniqueness assumption remains, and existing simple-event mapping stays green.</acceptance_criteria>
  <done>Linking and future updates can target one explicit recurring occurrence without modifying the series master accidentally.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

ASVS level 1; block on high. Untrusted SUMMARY/DESCRIPTION/ICS crosses pure parsers and becomes preview content.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-03-06 | Tampering | lossless editor | high | mitigate | allowlisted property mutation and exact unchanged-region golden tests |
| T-03-07 | Denial of service | ICS parser/editor | high | mitigate | explicit size/depth/line limits and fail-closed outcomes |
| T-03-08 | Spoofing | recurrence target | high | mitigate | resource identity plus exact UID+RECURRENCE-ID selector; ambiguity blocks output |
| T-03-09 | Information disclosure | fixtures/errors | high | mitigate | anonymous fixtures and errors without payload bodies |
| T-03-10 | Tampering | rendered OS text | medium | mitigate | deterministic typed renderer and date-divergence acknowledgement |
| T-03-SC | Tampering | package installation | low | accept | no new dependency or install action |
</threat_model>

<verification>
Focused golden suites after each task; wave gate `./gradlew testDebugUnitTest assembleDebug`. Use Android Studio's bundled JDK if the documented loopback daemon issue recurs.
</verification>

<success_criteria>
- Every supported input keeps its raw source and produces conservative fields.
- Update/final descriptions are deterministic and non-duplicating.
- Only four authorized properties change in an existing VEVENT.
- Explicit recurrence exceptions and invited fields survive exactly.
</success_criteria>

## Artifacts this phase produces

- `ServiceOrderExtractor`, `ExtractedField`, `ExtractionConfidence`, `ServiceOrderValidation`, `ServiceOrderRenderer`.
- `IcsDocument`, `IcsComponentSelector`, `IcsDocumentEditor`, typed lossless edit outcomes.
- Two anonymized minimal fixture files and their golden suites.
- `RemoteEventOccurrenceEntity`, `RemoteEventOccurrenceDao`, occurrence-aware `RemoteEventMapper`.

<output>
Create `.planning/phases/03-ordens-de-servico-publicacao-controlada/03-02-SUMMARY.md` when done.
</output>
