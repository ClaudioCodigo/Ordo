---
phase: 03-ordens-de-servico-publicacao-controlada
plan: 05
type: execute
wave: 5
depends_on: [03-04]
files_modified:
  - app/src/main/java/dev/claudiocodigo/nexo/ui/screens/oseditor/ServiceOrderEditorViewModel.kt
  - app/src/main/java/dev/claudiocodigo/nexo/ui/screens/oseditor/ServiceOrderEditorState.kt
  - app/src/test/java/dev/claudiocodigo/nexo/ui/screens/oseditor/ServiceOrderEditorViewModelTest.kt
  - app/src/main/java/dev/claudiocodigo/nexo/data/preferences/RecentServiceOrderPreferences.kt
  - app/src/main/java/dev/claudiocodigo/nexo/ui/screens/oseditor/ServiceOrderEditorScreen.kt
  - app/src/main/java/dev/claudiocodigo/nexo/ui/screens/oseditor/ServiceOrderSections.kt
  - app/src/androidTest/java/dev/claudiocodigo/nexo/ui/screens/oseditor/ServiceOrderEditorScreenTest.kt
  - app/src/main/java/dev/claudiocodigo/nexo/ui/screens/preview/PublicationPreviewViewModel.kt
  - app/src/main/java/dev/claudiocodigo/nexo/ui/screens/preview/PublicationPreviewScreen.kt
  - app/src/main/java/dev/claudiocodigo/nexo/ui/navigation/Routes.kt
  - app/src/main/java/dev/claudiocodigo/nexo/MainActivity.kt
  - app/src/androidTest/java/dev/claudiocodigo/nexo/NavigationTest.kt
autonomous: true
requirements: [OS-02, OS-03, OS-04, OS-05, OS-06, OS-07, OS-08]
estimate:
  tokens: 46000
  raw_tokens: 46000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "D-07/D-08/D-09: one editor/ViewModel owns the draft; collapsible stateless sections expose both presets and repeatable generic item records."
    - "D-10/D-13: preview is complete and read-only, showing text/date/time/calendar; only explicit confirmation creates an immutable publication operation."
    - "D-11/D-12: autosave has no duplicate save button; fixed bottom action is exactly Publish, Send update or Finish for current state; date is textual and time stays native."
    - "D-14: confirming offline queues the exact preview; continued edits affect only the draft and pending replacement requires cancellation/new confirmation."
    - "R3 edge coverage is visible in UI: empty required values block remote action, not local autosave; Unicode and latest revision survive navigation/process lifecycle."
    - "R4/R5 edge coverage is visible in UI: empty update/final fields are rejected, equal updates remain separate, explicit Nenhuma pendency is accepted and local history survives completion."
  prohibitions:
    - statement: "No autosave/open/cancel-preview action publishes remotely."
      status: "enforced"
      verification: "ViewModel/navigation tests assert zero confirm/outbox call until Confirmar publicação."
    - statement: "No user correction edits a second free-text final description or deletes source/history."
      status: "enforced"
      verification: "Preview has no text input; all edits route to structured draft commands."
    - statement: "Finishing does not mark external validation or write green."
      status: "enforced"
      verification: "UI shows Aguardando validação externa after confirmed completion."
    - statement: "Conflict does not silently retry stale preview."
      status: "enforced"
      verification: "Conflict outcome navigates away from success and requires later conflict-review flow."
  artifacts:
    - path: "app/src/main/java/dev/claudiocodigo/nexo/ui/screens/oseditor/ServiceOrderEditorScreen.kt"
      provides: "Single collapsible structured editor"
    - path: "app/src/main/java/dev/claudiocodigo/nexo/ui/screens/preview/PublicationPreviewScreen.kt"
      provides: "Complete read-only confirmation gate"
  key_links:
    - from: "ServiceOrderEditorViewModel"
      to: "StructuredServiceOrderStore"
      via: "revision-safe debounced autosave and flush-on-exit"
      pattern: "revision|flushNow|saveMutex"
    - from: "PublicationPreviewViewModel.confirm"
      to: "PublicationRepository.confirmPreview"
      via: "immutable preview ID"
      pattern: "confirmPreview"
---

<objective>
Deliver the structured technician workflow: one maintainable editor, retained repeated selections, deterministic read-only preview and explicit remote-action confirmation.

Purpose: replace free-text/manual save ambiguity with a reliable offline-first OS form.
Output: editor state/UI, presets/items, preview confirmation and typed navigation.
</objective>

<execution_context>
@C:/Users/claudio.lima/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/claudio.lima/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-ordens-de-servico-publicacao-controlada/03-CONTEXT.md
@.planning/phases/03-ordens-de-servico-publicacao-controlada/03-PATTERNS.md
@.planning/phases/03-ordens-de-servico-publicacao-controlada/03-04-SUMMARY.md

<interfaces>
Use one editor state over Plan 01's draft aggregate, Plan 02's extractor/validation/renderer, and Plan 04's immutable confirmation API. Navigation 3 routes carry UUIDs only, never ICS/payload.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build one revision-safe editor state for linked and provisional OS</name>
  <read_first>NovaOSViewModel.kt; DetalhesViewModel.kt; ServiceOrderModels.kt; ServiceOrderRenderer.kt; PublicationRepository.kt</read_first>
  <files>ui/screens/oseditor/ServiceOrderEditorViewModel.kt, ui/screens/oseditor/ServiceOrderEditorState.kt, test/ui/screens/oseditor/ServiceOrderEditorViewModelTest.kt, data/preferences/RecentServiceOrderPreferences.kt</files>
  <behavior>
    - Existing/provisional UUID loads one authoritative Room draft; stale async load/save completion cannot replace a newer local edit.
    - Selecting preset changes labels/required fields only after explicit user choice; SUMMARY inference may surface a suggestion but does not auto-select.
    - Technician and company/unit recent values are offered on future drafts, while official number stays optional and SEM OS is presentation only.
    - Add/edit/remove update, pendency and generic item autosaves; flushNow persists latest revision on exit/stop.
  </behavior>
  <action>Port the proven NovaOS revision/debounce/mutex pattern into one shared ViewModel and immutable screen state. Room remains canonical; DataStore/preferences store only recent technician/company/unit convenience values. Expose explicit commands for every section and a derived applicable remote action. Do not expose manual save-draft or mutable final preview text.</action>
  <verify><automated>./gradlew testDebugUnitTest --tests '*ServiceOrderEditorViewModelTest'</automated></verify>
  <acceptance_criteria>Concurrency/autosave/reload/preset/recent-value tests pass; linked and provisional modes use one implementation.</acceptance_criteria>
  <done>The full structured draft can be safely edited offline without duplicate save semantics.</done>
</task>

<task type="auto">
  <name>Task 2: Render the single-screen collapsible editor and fixed remote action</name>
  <read_first>ServiceOrderEditorState.kt; NovaOSScreen.kt lifecycle flush pattern; Material 3 components already used by project</read_first>
  <files>ui/screens/oseditor/ServiceOrderEditorScreen.kt, ui/screens/oseditor/ServiceOrderSections.kt, androidTest/ui/screens/oseditor/ServiceOrderEditorScreenTest.kt</files>
  <action>Create a thin scaffold and stateless section composables for preset, identification, origin, updates, pendencies, items/equipment, completion and preview entry. Keep one section expanded at a time without losing state. Fixed bottom bar exposes only the derived `Publicar no calendário`, `Enviar atualização` or `Finalizar OS`; show autosave status but no disk/manual-save action. Preserve edge-to-edge/adaptive Material 3 patterns and accessible labels/testTags.</action>
  <verify>
    <automated>./gradlew assembleDebugAndroidTest</automated>
    <human-check>Inspect phone and tablet previews/device: keyboard does not cover bottom action; section expansion and repeatable item controls remain usable.</human-check>
  </verify>
  <acceptance_criteria>Instrumented UI test can edit each section, add two items/updates, rotate/recompose without loss, and reach exactly one applicable remote action.</acceptance_criteria>
  <done>The technician can maintain a complete structured OS on one coherent screen.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Gate every remote action through complete read-only preview</name>
  <read_first>Routes.kt; MainActivity.kt; NavigationTest.kt; PublicationRepository.kt; ServiceOrderRenderer.kt</read_first>
  <files>ui/screens/preview/PublicationPreviewViewModel.kt, ui/screens/preview/PublicationPreviewScreen.kt, ui/navigation/Routes.kt, MainActivity.kt, androidTest/NavigationTest.kt</files>
  <behavior>
    - Editor action navigates by OS/preview IDs to a screen showing exact generated text, execution date, start/end and destination calendar.
    - Back/cancel returns without outbox/network mutation; confirm inserts immutable version/operation then schedules drain.
    - Editing the draft after confirm does not change displayed/queued confirmed version; validation/date-divergence must be acknowledged before confirm.
    - Successful completion transitions internal state to Aguardando validação externa, not green/validated.
  </behavior>
  <action>Add typed routes and full-screen navigation for editor/preview, passing IDs only. Preview ViewModel loads a persisted immutable candidate or builds then persists it locally without publishing; confirmation is the only outbox command. Screen is selectable/copyable read-only text with explicit back and confirm. Update bottom-bar hiding for full-screen flows.</action>
  <verify><automated>./gradlew testDebugUnitTest --tests '*ServiceOrderEditorViewModelTest' assembleDebugAndroidTest</automated></verify>
  <acceptance_criteria>Navigation test covers provisional editor→preview→cancel and confirm; zero writer calls occur on open/cancel; confirmed payload remains immutable.</acceptance_criteria>
  <done>Every create/update/finalization requires a complete, explicit, reproducible confirmation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

ASVS level 1; block on high. User/ICS-derived fields cross UI→Room; explicit confirmation crosses UI→outbox.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-03-21 | Tampering | stale ViewModel writes | high | mitigate | revision counter, mutex, latest-save tests and Room authority |
| T-03-22 | Repudiation | publication confirmation | high | mitigate | immutable preview/version ID and explicit confirm action |
| T-03-23 | Information disclosure | preview/navigation | medium | mitigate | IDs only in routes; no credentials; preview scoped to selected OS |
| T-03-24 | Tampering | date divergence | medium | mitigate | visible warning and explicit acknowledgement without silent correction |
| T-03-25 | Spoofing | inferred preset/fields | medium | mitigate | confidence indicators and explicit preset/ambiguity confirmation |
| T-03-SC | Tampering | package installation | low | accept | existing Compose/Navigation/DataStore only |
</threat_model>

<verification>
Run ViewModel tests after Task 1, compile instrumented UI after Task 2, navigation/preview gate after Task 3. Wave gate `./gradlew testDebugUnitTest assembleDebug assembleDebugAndroidTest`.
</verification>

<success_criteria>
- Draft fields autosave and survive navigation/process lifecycle.
- Both presets and generic item blocks render correctly.
- No duplicated save control or editable final-text source exists.
- Every remote action requires complete preview and explicit confirm.
</success_criteria>

## Artifacts this phase produces

- `ServiceOrderEditorState`, `ServiceOrderEditorViewModel`, `ServiceOrderEditorScreen`, stateless section composables.
- `RecentServiceOrderPreferences` for non-canonical repeated selections.
- `PublicationPreviewViewModel`, `PublicationPreviewScreen`, editor/preview Navigation 3 routes and tests.

<output>
Create `.planning/phases/03-ordens-de-servico-publicacao-controlada/03-05-SUMMARY.md` when done.
</output>
