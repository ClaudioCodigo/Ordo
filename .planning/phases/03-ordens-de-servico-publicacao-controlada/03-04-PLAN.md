---
phase: 03-ordens-de-servico-publicacao-controlada
plan: 04
type: execute
wave: 4
depends_on: [03-03]
files_modified:
  - app/src/main/java/dev/claudiocodigo/nexo/domain/publication/PublicationModels.kt
  - app/src/main/java/dev/claudiocodigo/nexo/domain/publication/PublicationRepository.kt
  - app/src/main/java/dev/claudiocodigo/nexo/data/local/dao/PublicationOutboxDao.kt
  - app/src/main/java/dev/claudiocodigo/nexo/data/repository/RoomPublicationRepository.kt
  - app/src/androidTest/java/dev/claudiocodigo/nexo/data/repository/RoomPublicationRepositoryTest.kt
  - app/src/main/java/dev/claudiocodigo/nexo/domain/publication/PublicationCoordinator.kt
  - app/src/main/java/dev/claudiocodigo/nexo/data/publication/RoomPublicationCoordinator.kt
  - app/src/test/java/dev/claudiocodigo/nexo/data/publication/PublicationCoordinatorTest.kt
  - app/src/main/java/dev/claudiocodigo/nexo/data/worker/PublicationWorker.kt
  - app/src/main/java/dev/claudiocodigo/nexo/data/worker/PublicationScheduler.kt
  - app/src/test/java/dev/claudiocodigo/nexo/data/worker/PublicationWorkerTest.kt
  - app/src/main/java/dev/claudiocodigo/nexo/di/RepositoryModule.kt
autonomous: true
requirements: [OS-03, OS-06, OS-07, OS-08]
estimate:
  tokens: 46000
  raw_tokens: 46000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "D-13/D-14: confirmation atomically stores an immutable intended version and exactly one outbox operation; later draft edits cannot alter its payload."
    - "D-15/D-22: the drain revalidates remote ETag/operation capability before PUT; 403 persists denied state, 412 persists conflict, and neither is automatically retried."
    - "D-16: outbox exposes observable Pending/Sending/Failed/Conflict/Sent states plus cancellable unsent and retryable transient operations."
    - "R2/R7 edge coverage: stable UID/href/operation IDs and preconditions make worker repetition/process interruption idempotent; failure after accepted PUT is reconciled by GET before conflict classification."
    - "R8 edge coverage: sent history is unique by operation/version and repeated read sync does not create duplicate history."
  prohibitions:
    - statement: "No publication operation is created without confirmed preview."
      status: "enforced"
      verification: "Only confirmPreview transaction inserts outbox; autosave repository has no outbox insert."
    - statement: "No draft or historical evidence is deleted by sending/canceling."
      status: "enforced"
      verification: "Cancellation changes operation state only; immutable version and draft remain queryable."
    - statement: "No green/color mutation occurs during completion publication."
      status: "enforced"
      verification: "Coordinator uses only lossless description editor and conditional writer."
    - statement: "No blind retry occurs after 412 or ambiguous success."
      status: "enforced"
      verification: "Worker tests assert no second PUT and GET reconciliation before terminal classification."
  artifacts:
    - path: "app/src/main/java/dev/claudiocodigo/nexo/domain/publication/PublicationModels.kt"
      provides: "Immutable preview/version/outbox/conflict outcomes"
    - path: "app/src/main/java/dev/claudiocodigo/nexo/data/publication/RoomPublicationCoordinator.kt"
      provides: "Transactional confirm and one-attempt drain"
    - path: "app/src/main/java/dev/claudiocodigo/nexo/data/worker/PublicationWorker.kt"
      provides: "Connected unique outbox drainer"
  key_links:
    - from: "confirmPreview"
      to: "ServiceOrderVersion + PublicationOutboxEntity"
      via: "single Room transaction"
      pattern: "withTransaction"
    - from: "PublicationWorker"
      to: "PublicationCoordinator.drainNext"
      via: "operation ID/queue claim, never ICS WorkData"
      pattern: "drainNext|Result.retry"
---

<objective>
Make confirmed publication durable and idempotent online or offline, including process-death recovery, response-loss reconciliation and persisted terminal states.

Purpose: turn the narrow writer into an offline-first capability without letting WorkManager become the source of truth.
Output: immutable outbox, coordinator, unique connected worker/scheduler and exhaustive state/idempotency tests.
</objective>

<execution_context>
@C:/Users/claudio.lima/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/claudio.lima/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-ordens-de-servico-publicacao-controlada/03-RESEARCH.md
@.planning/phases/03-ordens-de-servico-publicacao-controlada/03-03-SUMMARY.md

<interfaces>
Consume immutable service-order version/outbox tables from Plan 01, render/editor outputs from Plan 02 and `CalDavWriteClient` typed outcomes from Plan 03. Existing SyncWorker shows typed WorkManager result mapping but conflict/permanent publication outcomes return success after persistence.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Atomically confirm immutable previews into an idempotent outbox</name>
  <read_first>ServiceOrderRecords.kt; ServiceOrderStoreDao.kt; 03-CONTEXT.md D-13/D-14/D-16; RoomCalendarSyncCoordinator transaction pattern</read_first>
  <files>domain/publication/PublicationModels.kt, domain/publication/PublicationRepository.kt, data/local/dao/PublicationOutboxDao.kt, data/repository/RoomPublicationRepository.kt, androidTest/data/repository/RoomPublicationRepositoryTest.kt</files>
  <behavior>
    - Confirming the same preview identity twice creates one version and one operation.
    - Editing the draft after confirmation leaves payload/digest/base ETag/target href/UID unchanged.
    - Atomic claim transitions one eligible operation to Sending with lease; concurrent claims return at most one owner.
    - Expired Sending lease becomes eligible; pending can be canceled; sent/conflict cannot be mutated back to pending by generic save.
  </behavior>
  <action>Define explicit preview snapshot, operation type/state/failure and queue item contracts. Implement a dedicated DAO/repository with unique constraints, transactional confirmation/claim/transition methods and Flow list. Store no credential, Authorization or WorkManager payload. Use injected ClockProvider and a documented lease duration. Avoid generic upsert for state transitions.</action>
  <verify><automated>./gradlew connectedDebugAndroidTest -Pandroid.testInstrumentationRunnerArguments.class=dev.claudiocodigo.nexo.data.repository.RoomPublicationRepositoryTest</automated></verify>
  <acceptance_criteria>Concurrency, immutability, cancellation, lease recovery and uniqueness tests pass against Room.</acceptance_criteria>
  <done>A confirmed preview survives offline/restart as one immutable, claimable operation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Drain one operation with revalidation, reconciliation and terminal evidence</name>
  <read_first>PublicationRepository.kt; CalDavReadClient.kt; CalDavWriteClient.kt; IcsDocumentEditor.kt; 03-RESEARCH.md Pattern 5</read_first>
  <files>domain/publication/PublicationCoordinator.kt, data/publication/RoomPublicationCoordinator.kt, test/data/publication/PublicationCoordinatorTest.kt</files>
  <behavior>
    - Before update, remote ETag/capability is checked; changed ETag persists base/local/current remote conflict without PUT.
    - A create/update attempt executes at most one PUT; transient transport keeps operation pending, 401 reconnect, 403 denied, 409 failed and 412 conflict are persisted.
    - Retry-time 412 or lost response triggers GET; matching UID and mutable semantic digest becomes reconciled success, mismatch becomes conflict.
    - Success captures/fetches new ETag, stores one published version relation and triggers read sync without manufacturing history duplicates.
  </behavior>
  <action>Implement a coordinator that claims one item, reads the immutable version, revalidates through the read client, checks exact privilege evidence, builds the lossless payload and calls the writer once. Persist every outcome before returning. Reconcile ambiguous/lost response semantically rather than comparing entire server-reserialized ICS. Error text must omit ICS/description/credentials.</action>
  <verify><automated>./gradlew testDebugUnitTest --tests '*PublicationCoordinatorTest'</automated></verify>
  <acceptance_criteria>Tests cover create/update success, offline, timeout, 401, 403, 409, 412, second concurrent change and response-loss reconciliation with recorded request counts.</acceptance_criteria>
  <done>One durable operation reaches a correct persisted result without unsafe retries.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Schedule one connected drain and map retry policy exactly</name>
  <read_first>SyncWorker.kt; SyncScheduler.kt; PublicationCoordinator.kt; RepositoryModule.kt</read_first>
  <files>data/worker/PublicationWorker.kt, data/worker/PublicationScheduler.kt, test/data/worker/PublicationWorkerTest.kt, di/RepositoryModule.kt</files>
  <behavior>
    - One unique WorkManager drain with NetworkType.CONNECTED is enqueued after confirmation/manual retry.
    - Empty/sent/conflict/permanent failure return success after state persistence; only network/timeout returns retry.
    - WorkData carries no ICS/description/credential; repeated workers cannot claim the same live lease.
  </behavior>
  <action>Create Hilt worker/scheduler following existing sync patterns, with one stable unique work name and exponential backoff. Bind publication repository/coordinator independently. Worker delegates all domain decisions and queue identity to Room; it must not construct or mutate payloads.</action>
  <verify><automated>./gradlew testDebugUnitTest --tests '*PublicationWorkerTest' assembleDebug</automated></verify>
  <acceptance_criteria>Worker policy tests pass for every outcome; one unique connected request is observable; no payload text appears in WorkData.</acceptance_criteria>
  <done>Offline confirmations resume safely when connectivity returns and terminal conflicts do not loop.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

ASVS level 1; block on high. Immutable local operations cross Room→worker→remote writer and remote outcomes return to Room.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-03-16 | Tampering | outbox idempotency | high | mitigate | stable IDs, unique indices, atomic confirm/claim and response reconciliation |
| T-03-17 | Tampering | stale ETag/412 | high | mitigate | preflight GET, If-Match and terminal persisted conflict with no retry |
| T-03-18 | Denial of service | worker retry loop | high | mitigate | retry only network/timeout; lease recovery and unique work |
| T-03-19 | Information disclosure | WorkData/outbox/errors | high | mitigate | IDs only in WorkData; no secrets/logged bodies/credential-shaped columns |
| T-03-20 | Elevation of privilege | invited event write | high | mitigate | exact capability preflight plus authoritative 403 persisted denial |
| T-03-SC | Tampering | package installation | low | accept | existing Room/WorkManager/OkHttp only |
</threat_model>

<verification>
Run focused DAO/coordinator/worker tests per task. Wave gate `./gradlew testDebugUnitTest assembleDebug assembleDebugAndroidTest`; execute Room instrumented tests on device/emulator before phase closure.
</verification>

<success_criteria>
- One confirmation produces one immutable operation and one eventual remote result.
- Worker interruption/repetition does not duplicate resources or versions.
- Stale bases/412/403 stop automatically and remain actionable.
- Only transient connectivity failures retry.
</success_criteria>

## Artifacts this phase produces

- `PublicationPreview`, `PublicationOperation`, `PublicationState`, `PublicationFailure`, `PublicationRepository`.
- `PublicationOutboxDao`, `RoomPublicationRepository`, atomic claim/transition operations.
- `PublicationCoordinator`, `RoomPublicationCoordinator`, typed drain outcomes.
- `PublicationWorker`, `PublicationScheduler`, Hilt bindings and focused tests.

<output>
Create `.planning/phases/03-ordens-de-servico-publicacao-controlada/03-04-SUMMARY.md` when done.
</output>
