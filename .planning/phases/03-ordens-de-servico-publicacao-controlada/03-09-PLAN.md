---
phase: 03-ordens-de-servico-publicacao-controlada
plan: 09
type: execute
wave: 9
depends_on: [03-08]
files_modified:
  - app/src/test/java/dev/claudiocodigo/nexo/SecurityPolicyTest.kt
  - app/src/test/java/dev/claudiocodigo/nexo/data/local/NexoDatabaseMigrationsConsistencyTest.kt
  - app/src/androidTest/java/dev/claudiocodigo/nexo/NavigationTest.kt
  - .planning/phases/03-ordens-de-servico-publicacao-controlada/03-VALIDATION.md
  - .planning/phases/03-ordens-de-servico-publicacao-controlada/03-UAT.md
autonomous: false
requirements: [OS-01, OS-02, OS-03, OS-04, OS-05, OS-06, OS-07, OS-08]
user_setup:
  - service: Nextcloud
    why: "Controlled real-server CalDAV validation after all automated gates pass"
    dashboard_config:
      - task: "Generate a temporary app password and enter/scan it only on the test device; choose an empty Saturday/Sunday slot in the work calendar"
        location: "Nextcloud account security settings and Nexo account screen"
      - task: "Revoke the temporary app password immediately after UAT"
        location: "Nextcloud account security settings"
estimate:
  tokens: 22000
  raw_tokens: 22000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "All R1-R8 edge resolutions remain green together: empty/Unicode/ordering/idempotency/concurrency cases preserve raw, draft, history and exact remote targeting."
    - "The real controlled flow creates exactly one [TESTE NEXO] resource, conditionally updates DESCRIPTION, and preserves SUMMARY/DTSTART/DTEND/LOCATION/COLOR/UID/recurrence/scheduling/unknown properties."
    - "A deliberate concurrent edit produces visible remote/local conflict with intact draft and requires a new preview/confirmation; a second conflict blocks again."
    - "A writable shared/invited resource succeeds when authorized; a denied one preserves local work and reports no permission without looping."
    - "Room 1→3 and 2→3, full unit suite, debug APK, androidTest APK and lint are green before real-server UAT."
  prohibitions:
    - statement: "No event or update is published without explicit preview confirmation."
      status: "verified"
      verification: "Security/navigation tests plus UAT cancel-preview check observe zero remote mutation."
    - statement: "No demand, draft or historical version is erased or silently replaced."
      status: "verified"
      verification: "Migration/repository/conflict tests and post-UAT local history inspection."
    - statement: "No internal completion is treated as external validation and no green is written."
      status: "verified"
      verification: "Writer API/source scan and real before/after COLOR comparison."
    - statement: "No 412 receives blind retry or stale published status."
      status: "verified"
      verification: "MockWebServer request counts plus deliberate real conflict/reconfirmation."
  artifacts:
    - path: ".planning/phases/03-ordens-de-servico-publicacao-controlada/03-UAT.md"
      provides: "Sanitized controlled-server evidence and pass/fail checklist"
    - path: ".planning/phases/03-ordens-de-servico-publicacao-controlada/03-VALIDATION.md"
      provides: "Nyquist-complete phase validation map"
  key_links:
    - from: "SecurityPolicyTest"
      to: "writer/outbox/worker/fixtures"
      via: "source/schema scan for secrets and forbidden API paths"
      pattern: "Authorization|appPassword|DELETE|COLOR"
    - from: "03-UAT.md"
      to: "03-COVERAGE.md capability rows"
      via: "create/update/conflict/permission evidence"
      pattern: "TESTE NEXO"
---

<objective>
Close Phase 3 with automated security/migration/navigation gates and one controlled real Nextcloud UAT using revocable credentials only on the device.

Purpose: verify server-specific ETag, permission, scheduling and reserialization behavior without risking production calendar data or repository secrets.
Output: green full suite, Nyquist sign-off and sanitized UAT evidence.
</objective>

<execution_context>
@C:/Users/claudio.lima/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/claudio.lima/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-ordens-de-servico-publicacao-controlada/03-SPEC.md
@.planning/phases/03-ordens-de-servico-publicacao-controlada/03-COVERAGE.md
@.planning/phases/03-ordens-de-servico-publicacao-controlada/03-VALIDATION.md
@.planning/phases/03-ordens-de-servico-publicacao-controlada/03-08-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Enforce final secret, migration, API-surface and navigation gates</name>
  <read_first>SecurityPolicyTest.kt; NexoDatabaseMigrationsConsistencyTest.kt; NavigationTest.kt; 03-COVERAGE.md; all Phase 3 summaries</read_first>
  <files>test/SecurityPolicyTest.kt, test/data/local/NexoDatabaseMigrationsConsistencyTest.kt, androidTest/NavigationTest.kt, .planning/phases/03-ordens-de-servico-publicacao-controlada/03-VALIDATION.md</files>
  <behavior>
    - Source/schema/fixture scan finds no credential, Authorization value/column, real hostname/ICS, sensitive log or credential-shaped outbox field.
    - Writer public surface/recorded requests contain create/update only, never DELETE/color/calendar/scheduling mutation or unconditional PUT.
    - Migration SQL/schema 3 consistency and navigation remote→start/continue→editor→preview/conflict plus Mais→Central remain green.
    - Every validation map row has an existing test and no three consecutive implementation tasks lack automated feedback.
  </behavior>
  <action>Extend the existing policy scan to every new caldav/publication/worker/fixture path using content-aware assertions that do not self-match comments. Add reflection/source/API assertions for forbidden writer surfaces. Finalize the migration consistency test against schema 3. Extend NavigationTest only for missing end-to-end route invariants. Run focused tests, then the complete verification suite; update 03-VALIDATION.md statuses/plan IDs and set Nyquist compliant only after evidence is green.</action>
  <verify>
    <automated>./gradlew testDebugUnitTest assembleDebug assembleDebugAndroidTest lintDebug</automated>
    <human-check>Run `./gradlew connectedDebugAndroidTest` on the attached device/emulator and confirm direct 1→3 and 2→3 migration cases pass.</human-check>
  </verify>
  <acceptance_criteria>Full host suite exits zero, instrumented suite passes on device/emulator, all Wave 0 gaps are closed, and validation frontmatter truthfully reads nyquist_compliant:true/wave_0_complete:true.</acceptance_criteria>
  <done>Automated evidence blocks unsafe server testing and covers every phase requirement/threat.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: Perform controlled weekend Nextcloud publication UAT and revoke credential</name>
  <precondition>All Task 1 automated and connected-device gates are green; a temporary Nextcloud app password is configured only on the device; selected test slot is Saturday or Sunday.</precondition>
  <read_first>03-COVERAGE.md Controlled-server validation; 03-VALIDATION.md Manual-Only Verifications; app build from Task 1</read_first>
  <files>.planning/phases/03-ordens-de-servico-publicacao-controlada/03-UAT.md</files>
  <action>Prepare a sanitized checklist in 03-UAT.md without URL, account, ICS body, ETag value or credential. Ask the user to: create one provisional event titled `[TESTE NEXO]` on Saturday/Sunday and confirm exactly one resource; cancel a separate preview and confirm zero resource; update the created event and compare preserved native/scheduling/unknown fields; make a remote edit after preview and confirm conflict/no loss/no blind retry, then review and reconfirm; test one authorized and one denied shared/invited event; verify queue/card/central states; revoke the temporary app password. Record only PASS/FAIL, dates in coarse form if desired, app version and non-sensitive observations. Do not operate on existing production events or copy real ICS into the repo.</action>
  <what-built>Um fluxo controlado de publicação/atualização CalDAV foi preparado com prévia explícita, condicionais ETag, reconciliação de conflito, preservação de rascunho e tratamento de permissão para um evento gravável e um negado; 03-UAT.md registra somente evidência sanitizada. Credenciais reais e ICS nunca entram no chat nem no repositório.</what-built>
  <how-to-verify>Use somente um aparelho de teste e uma agenda controlada no Nextcloud, com senha temporária mantida apenas no aparelho: (1) crie um único evento `[TESTE NEXO]` em sábado/domingo e confirme um único recurso; (2) cancele uma prévia separada e confirme que nenhum recurso foi criado; (3) atualize DESCRIPTION e compare SUMMARY, DTSTART, DTEND, LOCATION, COLOR, UID, recorrência, propriedades de scheduling e desconhecidas; (4) edite remotamente após a prévia e confirme 412/conflito visível, rascunho intacto, nenhuma tentativa cega e nova prévia/confirmação; (5) teste um evento compartilhado/convidado gravável e um negado, confirmando que a negação preserva o rascunho e interrompe novas tentativas; (6) verifique fila/cartão/central; (7) revogue a senha temporária imediatamente. Registre em 03-UAT.md apenas PASS/FAIL, data aproximada, versão e observações não sensíveis; não copie URL, conta, ICS ou ETag real.</how-to-verify>
  <resume-signal>Após concluir o procedimento controlado, responda exatamente “UAT Nextcloud aprovado” se todos os passos passarem, ou “UAT Nextcloud falhou: passo e observação sanitizada” para registrar a falha. Nunca envie credenciais, ICS real, URL, conta ou ETag no chat.</resume-signal>
  <verify>
    <automated>./gradlew testDebugUnitTest --tests '*CalDavWriteClientTest' --tests '*PublicationCoordinatorTest' --tests '*SecurityPolicyTest'</automated>
    <human-check>All controlled steps pass, before/after comparison shows only authorized mutable properties changed, exactly one resource exists, conflict preserves the draft, denied share does not loop, and the temporary password is visibly revoked.</human-check>
  </verify>
  <acceptance_criteria>03-UAT.md contains a complete sanitized PASS record or a precise failing step; credential is revoked; repository status shows no real ICS/secret/unrelated event modification.</acceptance_criteria>
  <done>The Phase 3 behavior is proven against the real server without exposing credentials or modifying unrelated calendar content.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

ASVS level 1; block on high. Real credentials/calendar data cross device↔Nextcloud only during gated UAT; repository receives sanitized evidence only.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-03-39 | Information disclosure | credential/ICS/log/repo | high | mitigate | temporary device-only password, policy scan, sanitized UAT and revocation |
| T-03-40 | Tampering | real calendar | high | mitigate | weekend `[TESTE NEXO]` resource only, conditional writes and before/after comparison |
| T-03-41 | Elevation of privilege | shared event | high | mitigate | authorized/denied tests, exact privilege preflight and authoritative 403 |
| T-03-42 | Repudiation | UAT result | medium | mitigate | versioned sanitized checklist with explicit PASS/FAIL evidence |
| T-03-43 | Denial of service | repeated real retries | high | mitigate | single controlled operations, no retry on 412/403, revoke credential at end |
| T-03-SC | Tampering | package installation | low | accept | no package installation |
</threat_model>

<verification>
Automated full suite and device migrations must pass before the blocking UAT checkpoint. If PowerShell Gradle hits the documented loopback daemon issue, run identical commands from Android Studio bundled JDK; never skip or weaken a gate.
</verification>

<success_criteria>
- Full automated/instrumented suite and lint are green.
- Exactly one controlled provisional event is created and safely updated.
- Conflict and invited-event permission behavior match persisted/local invariants.
- Temporary credential is revoked and no sensitive artifact enters Git.
</success_criteria>

## Artifacts this phase produces

- Expanded `SecurityPolicyTest`, schema-v3 consistency gate and full navigation coverage.
- Final `03-VALIDATION.md` Nyquist sign-off.
- Sanitized `03-UAT.md` controlled-server evidence with credential revocation confirmation.

<output>
Create `.planning/phases/03-ordens-de-servico-publicacao-controlada/03-09-SUMMARY.md` when done.
</output>
