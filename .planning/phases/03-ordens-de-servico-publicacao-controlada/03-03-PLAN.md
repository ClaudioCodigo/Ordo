---
phase: 03-ordens-de-servico-publicacao-controlada
plan: 03
type: execute
wave: 3
depends_on: [03-01, 03-02]
files_modified:
  - .planning/phases/03-ordens-de-servico-publicacao-controlada/03-COVERAGE.md
  - app/src/main/java/dev/claudiocodigo/nexo/domain/caldav/CalDavWriteClient.kt
  - app/src/main/java/dev/claudiocodigo/nexo/domain/caldav/CalDavModels.kt
  - app/src/main/java/dev/claudiocodigo/nexo/data/caldav/CalDavXmlParser.kt
  - app/src/test/java/dev/claudiocodigo/nexo/data/caldav/CalDavXmlParserTest.kt
  - app/src/main/java/dev/claudiocodigo/nexo/data/caldav/NextcloudCalDavWriteClient.kt
  - app/src/test/java/dev/claudiocodigo/nexo/data/caldav/CalDavWriteClientTest.kt
  - app/src/test/java/dev/claudiocodigo/nexo/data/caldav/CalDavReadOnlyRegressionTest.kt
  - app/src/main/java/dev/claudiocodigo/nexo/di/CalDavModule.kt
  - app/src/main/java/dev/claudiocodigo/nexo/data/repository/RoomCalendarSetupRepository.kt
autonomous: true
requirements: [OS-03, OS-05, OS-08]
estimate:
  tokens: 40000
  raw_tokens: 40000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "D-03/D-11/D-22: writer targets one href/occurrence, preserves native time/scheduling/unknown fields, and checks operation-specific privilege while treating HTTP response as authority."
    - "Create has only an If-None-Match:* path; update cannot be represented without a nonblank ETag and always emits If-Match."
    - "Phase 2 read client and HttpMethodAllowlist remain strictly read-only; writer exposes no DELETE, color, scheduling or unconditional generic request API."
    - "R6 edge coverage remains true across transport: absent optional properties stay absent and canceling/building a preview sends no request."
    - "R7 edge coverage: 412 is typed as conflict after at most one PUT in an attempt; network failure is typed transient; authorization/permission/server failures are distinct."
  prohibitions:
    - statement: "No new event/update is published without explicit technician confirmation."
      status: "enforced"
      verification: "Writer accepts only immutable confirmed request types; it is not called by editor/autosave."
    - statement: "Demand, drafts and historical versions are never removed by transport."
      status: "enforced"
      verification: "Writer is stateless and returns typed outcome; it has no local delete API."
    - statement: "Internal completion is not external validation and green is never written."
      status: "enforced"
      verification: "Public contract has no color argument; lossless payload test asserts original COLOR."
    - statement: "412 never triggers blind resend or published status."
      status: "enforced"
      verification: "MockWebServer observes one PUT and typed Conflict response."
  artifacts:
    - path: "app/src/main/java/dev/claudiocodigo/nexo/domain/caldav/CalDavWriteClient.kt"
      provides: "Conditional create/update-only API"
    - path: "app/src/main/java/dev/claudiocodigo/nexo/data/caldav/NextcloudCalDavWriteClient.kt"
      provides: "Same-origin conditional PUT adapter"
    - path: ".planning/phases/03-ordens-de-servico-publicacao-controlada/03-COVERAGE.md"
      provides: "Full CalDAV/WebDAV capability decision matrix"
  key_links:
    - from: "NextcloudCalDavWriteClient"
      to: "ConditionalCreate/ConditionalUpdate"
      via: "mandatory request headers"
      pattern: "If-None-Match|If-Match"
    - from: "NextcloudCalDavWriteClient"
      to: "CredentialStore"
      via: "same-origin authorization after URL validation"
      pattern: "expectedOrigin|Authorization"
---

<objective>
Add the only remote mutation boundary allowed in Phase 3: a narrow, conditional, same-origin CalDAV writer with exact privilege evidence and complete negative API coverage.

Purpose: enable safe creation/update without weakening Phase 2's zero-write client.
Output: capability matrix, exact privilege model, typed writer contract/implementation and MockWebServer security tests.
</objective>

<execution_context>
@C:/Users/claudio.lima/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/claudio.lima/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-ordens-de-servico-publicacao-controlada/03-COVERAGE.md
@.planning/phases/03-ordens-de-servico-publicacao-controlada/03-RESEARCH.md
@.planning/phases/03-ordens-de-servico-publicacao-controlada/03-02-SUMMARY.md

<interfaces>
Existing `CalDavReadClient` is read-only; existing `HttpMethodAllowlist.ALLOWED` is OPTIONS/PROPFIND/REPORT/GET/HEAD. New `CalDavWriteClient` must expose only `create(ConditionalCreate)` and `update(ConditionalUpdate)` returning typed `WriteOutcome`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Preserve exact DAV privileges and freeze the writer capability surface</name>
  <read_first>03-COVERAGE.md; CalDavModels.kt; CalDavXmlParser.kt; CalendarEntity.kt; CalDavReadClient.kt; HttpMethodAllowlist.kt</read_first>
  <files>03-COVERAGE.md, domain/caldav/CalDavWriteClient.kt, domain/caldav/CalDavModels.kt, data/caldav/CalDavXmlParser.kt, test/data/caldav/CalDavXmlParserTest.kt</files>
  <behavior>
    - XML discovery retains exact `bind`, `write-content`, `write-properties` names instead of collapsing on contains(write).
    - Create preflight requires bind on the collection; update preflight requires write-content when privilege evidence is available.
    - ConditionalUpdate construction rejects blank ETag; ConditionalCreate always represents require-absent and has a stable target href/UID.
    - Public writer API contains no DELETE, generic method, color or scheduling mutation.
  </behavior>
  <action>Write parser/API contract tests first. Extend domain capability evidence with exact privilege names while preserving existing compatibility fields only where callers still need them. Define immutable conditional request/outcome types and `CalDavWriteClient` with two methods. Keep `03-COVERAGE.md` synchronized with the exact symbols. Do not implement PUT in this task; the red-to-green gate here is parser and type contract only.</action>
  <verify><automated>./gradlew testDebugUnitTest --tests '*CalDavXmlParserTest'</automated></verify>
  <acceptance_criteria>Exact privileges round-trip namespace-aware XML; contract makes unconditional update unrepresentable; coverage matrix has a reasoned row for every retained/added/opted-out capability.</acceptance_criteria>
  <done>Executors and tests have a closed mutation API before production transport is enabled.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement same-origin conditional PUT with typed outcomes</name>
  <read_first>CalDavWriteClient.kt; CalDavHttpClient.kt; NextcloudCalDavReadClient.kt; IcsDocumentEditor.kt; 03-COVERAGE.md</read_first>
  <files>data/caldav/NextcloudCalDavWriteClient.kt, test/data/caldav/CalDavWriteClientTest.kt, test/data/caldav/CalDavReadOnlyRegressionTest.kt</files>
  <behavior>
    - Create sends text/calendar UTF-8 and If-None-Match:*; update sends If-Match with the exact base ETag.
    - Authorization is attached only to the configured HTTPS origin; cross-origin/downgrade redirects fail without credential forwarding.
    - 200/201/204 capture ETag or return a typed need-readback state; 401/403/409/412/5xx/timeout/network map distinctly.
    - 412 causes no second PUT; no DELETE request can be produced; existing read allowlist regression remains green.
  </behavior>
  <action>Implement a writer-specific OkHttp adapter reusing the proven origin/redirect/timeout ideas but not the read-only guard interceptor. Zero temporary password char arrays where feasible and never log request bodies, descriptions or Authorization. Require the typed request preconditions at the adapter boundary. Keep automatic redirect handling disabled and validate each Location before another request.</action>
  <verify><automated>./gradlew testDebugUnitTest --tests '*CalDavWriteClientTest' --tests '*CalDavReadOnlyRegressionTest'</automated></verify>
  <acceptance_criteria>MockWebServer proves headers, bodies, status mapping, credential containment, one-PUT 412 behavior and unchanged Phase 2 read restrictions.</acceptance_criteria>
  <done>Conditional create/update can be called safely by a coordinator, and no broader remote mutation path exists.</done>
</task>

<task type="auto">
  <name>Task 3: Bind writer separately and persist capability evidence</name>
  <read_first>CalDavModule.kt; DatabaseModule.kt; ServiceOrderRecords.kt calendar-capability record; RoomCalendarSetupRepository.kt; outputs of Tasks 1-2</read_first>
  <files>data/repository/RoomCalendarSetupRepository.kt, di/CalDavModule.kt, test/data/caldav/CalDavWriteClientTest.kt</files>
  <action>Add a separate Hilt binding/provider for `CalDavWriteClient`; do not replace the read binding. Persist exact capability names through the calendar-capability record created in Plan 01 using a deterministic serialized set/converter. Add a construction/integration test proving read and write clients coexist and that no writer is reachable through `CalDavReadClient`.</action>
  <verify><automated>./gradlew testDebugUnitTest --tests '*CalDavWriteClientTest' assembleDebug</automated></verify>
  <acceptance_criteria>Hilt build succeeds, selected calendar exposes exact privileges, and read-only interfaces/allowlist remain unmodified.</acceptance_criteria>
  <done>The mutation boundary is injectable only where publication explicitly depends on it.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

ASVS level 1; block on high. Credentials cross app→HTTPS origin; DAV XML and HTTP outcomes cross server→app.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-03-11 | Information disclosure | redirects/Authorization | high | mitigate | manual same-origin HTTPS redirects and MockWebServer credential tests |
| T-03-12 | Tampering | conditional update/create | high | mitigate | mandatory If-Match/If-None-Match types and negative API tests |
| T-03-13 | Elevation of privilege | shared/invited resource | high | mitigate | operation-specific privilege hint plus authoritative 403 outcome |
| T-03-14 | Denial of service | DAV XML/HTTP body | medium | mitigate | existing secure namespace parser, size/time limits and typed failures |
| T-03-15 | Information disclosure | logs/errors | high | mitigate | no body/Authorization logging; security scan in final plan |
| T-03-SC | Tampering | package installation | low | accept | no external package installation |
</threat_model>

<verification>
Run focused MockWebServer/XML tests after each task and `./gradlew testDebugUnitTest assembleDebug` at wave end. Use Android Studio's JDK if the PowerShell Gradle daemon cannot establish loopback.
</verification>

<success_criteria>
- Every create/update request carries the correct atomic precondition.
- Cross-origin credentials never leave the configured server origin.
- 412 and permissions are typed, non-retrying outcomes.
- DELETE/color/calendar/scheduling mutations have no public or HTTP path.
</success_criteria>

## Artifacts this phase produces

- `CalDavWriteClient`, `ConditionalCreate`, `ConditionalUpdate`, `WriteOutcome`.
- Exact DAV privilege evidence in domain/storage.
- `NextcloudCalDavWriteClient` and MockWebServer contract/security suite.
- Maintained `03-COVERAGE.md` full capability matrix.

<output>
Create `.planning/phases/03-ordens-de-servico-publicacao-controlada/03-03-SUMMARY.md` when done.
</output>
