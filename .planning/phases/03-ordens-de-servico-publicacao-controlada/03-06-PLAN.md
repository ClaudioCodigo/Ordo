---
phase: 03-ordens-de-servico-publicacao-controlada
plan: 06
type: execute
wave: 6
depends_on: [03-05]
files_modified:
  - app/src/main/java/dev/claudiocodigo/nexo/domain/serviceorder/ServiceOrderDiff.kt
  - app/src/test/java/dev/claudiocodigo/nexo/domain/serviceorder/ServiceOrderDiffTest.kt
  - app/src/main/java/dev/claudiocodigo/nexo/ui/screens/conflito/ConflictReviewViewModel.kt
  - app/src/main/java/dev/claudiocodigo/nexo/ui/screens/conflito/ConflictReviewScreen.kt
  - app/src/test/java/dev/claudiocodigo/nexo/ui/screens/conflito/ConflictReviewViewModelTest.kt
  - app/src/main/java/dev/claudiocodigo/nexo/ui/screens/remoto/RemoteEventDetailViewModel.kt
  - app/src/main/java/dev/claudiocodigo/nexo/ui/screens/remoto/RemoteEventDetailScreen.kt
  - app/src/main/java/dev/claudiocodigo/nexo/ui/navigation/Routes.kt
  - app/src/main/java/dev/claudiocodigo/nexo/MainActivity.kt
  - app/src/androidTest/java/dev/claudiocodigo/nexo/NavigationTest.kt
autonomous: true
requirements: [OS-01, OS-02, OS-03, OS-08]
estimate:
  tokens: 39000
  raw_tokens: 39000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "D-01/D-02: card opens remote detail read-only; only Iniciar atendimento creates the local copy and Continuar atendimento opens the same linked UUID."
    - "D-04/D-06: start-attendance prefills confident SUMMARY/DESCRIPTION fields and presents ambiguous extraction for confirmation while preserving raw input."
    - "D-05/D-17: remote changes are compared semantically field-by-field against base/local; only changed/new fields ask Manter meu valor or Usar valor do calendário; full descriptions can expand."
    - "D-18: applying choices updates draft/base but never mutates blocked operation; user may edit, then must generate/confirm a new preview with renewed ETag; another change blocks again."
    - "D-22: invited/shared fields remain evidence; local attendance is allowed even when remote update permission is denied."
    - "R1/R7/R8 edge coverage: duplicate starts are idempotent, malformed/empty remote fields remain raw, conflict preserves all three versions and repeated cycles do not duplicate history."
  prohibitions:
    - statement: "Opening remote detail or continuing attendance never publishes automatically."
      status: "enforced"
      verification: "Navigation/ViewModel tests record zero outbox/writer calls before preview confirmation."
    - statement: "Conflict review never silently discards base, local draft, remote current or historical operation."
      status: "enforced"
      verification: "Tests query all references after field-choice application."
    - statement: "Conflict resolution never writes color or equates internal completion to validation."
      status: "enforced"
      verification: "Diff/choice model has no COLOR mutation field."
    - statement: "412/stale conflict never causes automatic reapply."
      status: "enforced"
      verification: "Apply choices returns editor state only; new preview+confirm is mandatory."
  artifacts:
    - path: "app/src/main/java/dev/claudiocodigo/nexo/domain/serviceorder/ServiceOrderDiff.kt"
      provides: "Typed base/local/remote semantic differences"
    - path: "app/src/main/java/dev/claudiocodigo/nexo/ui/screens/conflito/ConflictReviewScreen.kt"
      provides: "Manual field-choice recovery"
    - path: "app/src/main/java/dev/claudiocodigo/nexo/ui/screens/remoto/RemoteEventDetailScreen.kt"
      provides: "Read-only start/continue attendance entry"
  key_links:
    - from: "RemoteEventDetailViewModel.startOrContinue"
      to: "StructuredServiceOrderStore.createOrGetAttendance"
      via: "exact resource/occurrence identity"
      pattern: "createOrGetAttendance"
    - from: "ConflictReviewViewModel.applyChoices"
      to: "draft + renewed base snapshot"
      via: "explicit per-field decisions in one local transaction"
      pattern: "applyChoices|renewedEtag"
---

<objective>
Complete safe entry and manual recovery: remote detail stays read-only, start/continue is idempotent, and concurrent changes are reviewed field-by-field without losing or automatically sending anything.

Purpose: let the technician work with shared changing events while retaining control.
Output: semantic diff, conflict review UI and remote-detail start/continue navigation.
</objective>

<execution_context>
@C:/Users/claudio.lima/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/claudio.lima/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-ordens-de-servico-publicacao-controlada/03-CONTEXT.md
@.planning/phases/03-ordens-de-servico-publicacao-controlada/03-05-SUMMARY.md

<interfaces>
Consume occurrence-aware create-or-get from Plan 01, extraction from Plan 02, persisted conflict triples from Plan 04 and editor/preview routes from Plan 05. Never pass raw ICS through navigation.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Compute conservative field-level base/local/remote differences</name>
  <read_first>ServiceOrderExtractor.kt; ServiceOrderModels.kt; 03-CONTEXT.md D-05/D-17/D-18</read_first>
  <files>domain/serviceorder/ServiceOrderDiff.kt, test/domain/serviceorder/ServiceOrderDiffTest.kt</files>
  <behavior>
    - Only fields changed from the saved base appear; remote unstructured text compares as the selected origin field rather than line diff.
    - Each difference carries base/local/remote and allowed explicit choices; equal values do not appear.
    - Raw full remote/local description is available as support but not used for automatic merge.
    - Applying a complete choice set returns a new local draft and renewed base/ETag while leaving old objects unchanged.
  </behavior>
  <action>Implement a pure typed semantic diff and choice applicator. Compare identification, origin, updates, pendencies and generic items at their structured boundaries. Do not auto-merge non-conflicting fields beyond omitting unchanged fields, infer intent, or mutate input objects. Validate that every displayed divergence has an explicit decision before application.</action>
  <verify><automated>./gradlew testDebugUnitTest --tests '*ServiceOrderDiffTest'</automated></verify>
  <acceptance_criteria>Tests cover raw-vs-structured origin, colon/newline formatting differences, all field choices, incomplete choices and immutable inputs.</acceptance_criteria>
  <done>Concurrent event versions can be compared by meaning rather than presentation lines.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Review conflict and return to editable draft under renewed base</name>
  <read_first>ServiceOrderDiff.kt; PublicationModels.kt conflict state; ServiceOrderEditorViewModel.kt; UI UDF patterns</read_first>
  <files>ui/screens/conflito/ConflictReviewViewModel.kt, ui/screens/conflito/ConflictReviewScreen.kt, test/ui/screens/conflito/ConflictReviewViewModelTest.kt</files>
  <behavior>
    - Screen labels Version remote/Version local per changed field and expands complete descriptions on demand.
    - Apply remains disabled until every divergence has Keep mine/Use calendar; no default is silently chosen.
    - Apply persists renewed base and draft, keeps blocked operation immutable, then opens editor; publishing requires new preview/confirmation.
    - A second remote change after review creates another conflict instead of overwriting.
  </behavior>
  <action>Create UDF ViewModel/screen with stable testTags and accessible choice controls. Load IDs from Room, never payload route args. Persist choices atomically as a new draft revision plus base snapshot; retain conflict/operation history. Surface forbidden/unauthorized separately from conflict and preserve all user text.</action>
  <verify><automated>./gradlew testDebugUnitTest --tests '*ConflictReviewViewModelTest' assembleDebug</automated></verify>
  <acceptance_criteria>All four behaviors pass; tests prove no outbox insert/new PUT until later preview confirmation.</acceptance_criteria>
  <done>The technician can consciously rebase local work and continue editing without data loss.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Wire read-only remote detail to idempotent start/continue flow</name>
  <read_first>RemoteEventDetailViewModel.kt; RemoteEventDetailScreen.kt; Routes.kt; MainActivity.kt; NavigationTest.kt</read_first>
  <files>ui/screens/remoto/RemoteEventDetailViewModel.kt, ui/screens/remoto/RemoteEventDetailScreen.kt, ui/navigation/Routes.kt, MainActivity.kt, androidTest/NavigationTest.kt</files>
  <behavior>
    - Opening a remote card reads/display only and shows Iniciar atendimento or Continuar atendimento based on exact link.
    - Repeated/rapid start calls create one local OS and navigate to the same editor ID.
    - If ETag changed since link/base, continue routes to changed-field review before editor; unchanged opens editor directly.
    - Denied invited event still permits local start/continue and communicates remote permission only at publication.
  </behavior>
  <action>Keep remote detail's load path read-only. Add bottom primary action (and optional overflow duplicate) driven by link state. On start, call transactional create-or-get, run conservative extraction and store snapshot before navigating. On continue, revalidate/read current remote and branch to conflict review without mutating the draft. Add typed conflict route and extend navigation tests.</action>
  <verify><automated>./gradlew assembleDebugAndroidTest</automated></verify>
  <acceptance_criteria>Navigation test covers remote→start→editor, reopen→continue same ID, changed ETag→review, and zero remote mutation during all entry paths.</acceptance_criteria>
  <done>Remote events become safe local work only after explicit user action and remain recoverable across concurrent edits.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

ASVS level 1; block on high. Untrusted/current remote content crosses read cache→diff UI→explicit local choice.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-03-26 | Tampering | conflict resolution | high | mitigate | immutable inputs, explicit choice per field, atomic local rebase and renewed ETag |
| T-03-27 | Spoofing | remote occurrence identity | high | mitigate | account/calendar/href plus UID+RECURRENCE-ID lookup |
| T-03-28 | Repudiation | choice history | medium | mitigate | retain blocked operation/base/remote and record chosen revision |
| T-03-29 | Information disclosure | conflict screen | medium | mitigate | IDs in routes; no credentials or unrelated event payloads |
| T-03-30 | Elevation of privilege | invited event | high | mitigate | local-only attendance allowed; publication remains capability/HTTP-gated |
| T-03-SC | Tampering | package installation | low | accept | no package installation |
</threat_model>

<verification>
Run diff and ViewModel suites after Tasks 1-2, navigation build after Task 3. Wave gate `./gradlew testDebugUnitTest assembleDebug assembleDebugAndroidTest`.
</verification>

<success_criteria>
- Remote detail never creates or writes without explicit start.
- Start/continue always resolves to one local UUID.
- Changed fields require explicit remote/local choices.
- Reapplication always returns through editor and a new preview/confirmation.
</success_criteria>

## Artifacts this phase produces

- `ServiceOrderDiff`, `ServiceOrderFieldDifference`, `ConflictChoice`, immutable choice applicator.
- `ConflictReviewViewModel`, `ConflictReviewScreen`, conflict route/tests.
- Evolved `RemoteEventDetailViewModel/Screen` with `Iniciar atendimento`/`Continuar atendimento`.

<output>
Create `.planning/phases/03-ordens-de-servico-publicacao-controlada/03-06-SUMMARY.md` when done.
</output>
